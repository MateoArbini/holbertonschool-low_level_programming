#include "main.h"
/**
 *print_most_numbers - print numbers between 0 - 9 except 4 and 2
 *@x: number
 **/
void print_most_numbers(void)
{
	int x;

	for (x = 0; x <= 9; x++)
		if (x = 2)
		{
			continue;
		}
		else if (x = 4)
		{
			continue;
		}
	_puthcar (x);
	_putchar ('\n');
}
