#include "main.h"
/**
 *main - print the programm name followed by a new line
 *@argc: unused var int
 *@argv: array of pointers that point to a string type char
 *Return: always 0
 **/
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
