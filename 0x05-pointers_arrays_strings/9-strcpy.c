#include "main.h"
/**
 *_strcpy - copies the string pointed to by src including null to buffer to de
 *@dest: return value of the point
 *@src: string pointed
 **/
char *_strcpy(char *dest, char *src)
{
	int i = 0;

	while (str[i] != '\0')
	{
		_putchar(str[i]);
		i++;
	}
	 _putchar('\n');

for 
