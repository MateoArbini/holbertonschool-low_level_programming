#include <stdio.h>
#include <stdlib.h>
#include "search_algos.h"

/**
 * binary_search - function that search for a value stored in a sorted array
 * of ints using the binary search algorithm
 * @array: pointer to the first element or the array
 */

int binary_search(int *array, size_t size, int value)
{
}
